# P2 Tasks — Audit Mode Polish (module control + portals + needs-review UI + multi-viewport + auth)

Spec: WORKBENCH_VISION.md §3. Branch: `phase/p2-modules`. Verify gates: module-toggle unit tests; regulation-mapping fixture.

## Tasks

1. **Module selection state model + persistence.** Extend `src/lib/audit-modules.ts`: add `serializeModuleSelection(sels)` / `parseModuleSelection(str)` (URL-safe encoded string) so config survives the `/audit-config` → audit-run handoff. Add `applyPresetToSelection(presetId, current)` that keeps required modules on. Test: `__tests__/audit-modules.test.ts` — toggling optional off keeps required on; preset apply yields exact preset id set; parse round-trips.
2. **Module-toggle behavior hardening.** `src/components/audit/ModuleSelector.tsx`: when required module is `enabled:false` from props, force-render as enabled; `toggle()` on required is a no-op (already true) — add a unit test asserting `onChange` is NOT called when toggling a required module. Verify gate: `module-toggle-required-locked.test.tsx` (jsdom) clicks `module-automated` and asserts parent state unchanged.
3. **Preset ↔ module sync (two-way).** In `src/app/audit-config/page.tsx`, when the user manually toggles a module such that the active set no longer equals any preset, set `activePreset` to `null` (currently stays stale). Add `matchPresetFromSelection(sels): string | null` in `audit-modules.ts`. Test: toggling keyboard off from "standard" flips preset badge to "Custom".
4. **Needs-review UI.** New `src/components/audit/NeedsReviewPanel.tsx`: filterable list of axe `incomplete` results grouped by rule; an "Accept as finding" / "Dismiss (false positive)" control per row that writes back to a `reviewDecision` field on the finding shape (extend `src/engine/axe-scan.ts` `AxeFinding` with optional `review?: "accept"|"dismiss"|"pending"`; default `"pending"`). Test: `NeedsReviewPanel.test.tsx` renders 3 incomplete findings, accepts 1, asserts accept count = 1, pending = 2.
5. **Portal session import.** Wire `src/lib/portal-sessions.ts` into the audit form: new `src/components/audit/PortalSessionPicker.tsx` listing saved sessions (stub client `listPortalSessions()` in `src/lib/portal-sessions.ts`; real Supabase binding lands in SETUP). Selecting a session sets target URL + attaches session id to the audit config payload. Test: `PortalSessionPicker.test.tsx` shows the 2 fixture sessions, selecting one updates the shown URL.
6. **Portal session creation form.** New `src/components/audit/PortalSessionForm.tsx` (basic / cookie / header only — SSO out of scope this phase, UI disabled with "coming soon"). Validate auth method via `isAuthMethod`. Test: empty name blocks submit; invalid method shows error.
7. **Multi-viewport module activation.** `responsive` module exists in `AUDIT_MODULES`; ensure `src/engine/browser.ts` `launchBrowserContext` accepts a `viewport` arg and that the audit-run path scans at 375/768/1280 when `responsive` enabled, producing per-viewport findings (tag `finding.viewport`). Test: `multi-viewport.test.ts` mocks launch and asserts 3 contexts created when responsive on, 1 when off.
8. **Auth on audit-config route.** Wrap `src/app/audit-config/page.tsx` in an auth guard — redirect to `/auth` when no session (use existing `src/lib/enterprise/auth.ts` `requireUser()` helper; add if missing). Test: `audit-config-guard.test.ts` renders with no session → redirect to `/auth`.
9. **Regulation-mapping fixture + export.** Add a "Regulations" card to audit-config summary showing which regulations the current module set covers (use `getModuleWcagCoverage` + `getScRegulationCoverage` from `regulation-mapping.ts`). Test: `regulation-coverage.fixtures.test.ts` asserts that selecting "Full" maps to all 8 regulations in `REGULATION_MAPPINGS`; selecting Quick maps to ≥ 4.
10. **E2E smoke (browser).** Playwright test `e2e/audit-config.spec.ts`: open `/audit-config`, apply "Compliance" preset, assert module counts in summary, toggle touch-targets off, assert preset badge → Custom, assert all 4 quick preset buttons are keyboard-focusable.

## Verify gates mapping
- module-toggle unit tests → tasks 2, 3
- regulation-mapping fixture → task 9
- npm run verify (lint + typecheck + unit) → all
- browser tests with CHROME_EXECUTABLE_PATH → task 10